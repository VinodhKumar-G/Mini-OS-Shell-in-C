# Mini-OS-Shell-in-C
“A mini OS shell built in C featuring command execution, parsing, built-in commands, history, undo, background jobs, redirection, piping, and environment variable handling using stack, queue, and linked list data structures. Demonstrates OS concepts and modular system programming.
